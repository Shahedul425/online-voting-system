global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "ovsapp"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8080"]  # IntelliJ dev
      - targets: ["app:8080"]                   # full stack mode (profile full)
